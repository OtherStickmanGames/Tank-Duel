namespace TankDuel.Data
{
    /// <summary>
    /// Задел под перки с фарм-ботов (задача 5.3). Пока в игре не используются.
    /// Эффекты перков будут применяться в TankBuild.ComputeStats —
    /// геймплейный код про конкретные перки знать не должен.
    /// </summary>
    public enum PerkType
    {
        None = 0,
    }
}
